#include <stdio.h>

int main(){
    double a,b,c;
    double p1=2,p2=3,p3=5;
    scanf("%lf",&a);
    scanf("%lf",&b);
    scanf("%lf",&c);
    printf("MEDIA = %.1lf\n",((a*p1)+(b*p2)+(c*p3))/(p1+p2+p3));
    return 0;
}
